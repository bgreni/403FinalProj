#include <gmpxx.h>
#include <iostream>
#include <math.h>
#include "utils.h"

using namespace std;

int main() {
    mpz_class n = 315;
//    mpz_pow_ui(n.get_mpz_t(), n.get_mpz_t(), 10);
//    n = sqrt(n);
//    double m = 1000;
//    m = sqrt(m);
//    get_factor_base(n,n);
    cout << n << endl;
}